import React, { Component } from 'react';

export default class Nomatch extends Component {
    render() {
        return (
            <div>
                login Page.
            </div>
        );
    }
}